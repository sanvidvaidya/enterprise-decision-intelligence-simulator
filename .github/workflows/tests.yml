name: tests
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install -r requirements.txt
      - run: python -m decision_intelligence.generate_data
        env: { PYTHONPATH: src }
      - run: python -m decision_intelligence.ingestion
        env: { PYTHONPATH: src }
      - run: python -m pytest
        env: { PYTHONPATH: src }